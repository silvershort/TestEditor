package com.example.testeditor

/*
import android.net.Uri
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.os.Environment
import com.video.trimmer.interfaces.OnCropVideoListener
import kotlinx.android.synthetic.main.activity_crop.*
import java.io.File

class CropActivity : AppCompatActivity(), OnCropVideoListener {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_crop)

        val path:String? = intent.getStringExtra("path")

        crop_cropper.setVideoURI(Uri.parse(path))
            .setOnCropVideoListener(this)
            .setMinMaxRatios(0.3f, 3f)
            .setDestinationPath(Environment.getExternalStorageDirectory().toString() + File.separator + "temp")
    }

    override fun cancelAction() {
        TODO("Not yet implemented")
    }

    override fun getResult(uri: Uri) {
        TODO("Not yet implemented")
    }

    override fun onCropStarted() {
        TODO("Not yet implemented")
    }

    override fun onError(message: String) {
        TODO("Not yet implemented")
    }

    override fun onProgress(progress: Float) {
        TODO("Not yet implemented")
    }
}
*/
